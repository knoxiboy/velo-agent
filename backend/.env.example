# ============================================================
# Velo — Backend Environment Variables
# Copy this file to .env and fill in your real values.
# NEVER commit your actual .env file to version control.
# ============================================================

# Google Gemini API key (required — Node 2 LLM Solver will fail without this)
# Get yours at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Flask runtime settings
PORT=5000
FLASK_ENV=development        # Set to "production" for live deployments

# Git commit identity used for all [AI-AGENT] commits
# Falls back to the machine's global git config if omitted
GIT_AUTHOR_NAME=oyelurker
GIT_AUTHOR_EMAIL=oyelurker@gmail.com

# Maximum number of heal-and-retest iterations (default: 5)
MAX_RETRIES=5

# Optional: restrict CORS origins in production (comma-separated)
# ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://yourdomain.com

# GitHub OAuth (backend handles login — no Firebase)
# Create an OAuth App at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
# Where to send the user after login (no trailing slash)
FRONTEND_URL=http://localhost:5173
# Optional: secret to sign JWTs (defaults to a dev value)
# JWT_SECRET=your_random_secret
# Optional: GitHub Personal Access Token for auto PR creation after healing
# Generate at: https://github.com/settings/tokens  (needs repo scope)
# If omitted, the PR step is silently skipped — everything else still works.
# GITHUB_TOKEN=ghp_your_token_here
